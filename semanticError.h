/**
 * @file semanticError.h
 * @author Vladimír Hucovič
 * @brief header file for semantic error function
 * 
 */
#pragma once
#include <stdlib.h>
#include <stdio.h>

/**
 * @brief exits program with semantic error code
 * 
 * @param code error code
 */
void semanticError(int code);